<p id="notice"><%= notice %></p>

<h1 align=center>Menu</h1>

<% @courses.each do |course| %>
  <h2 align=center ><%=course.name%></h2>
  <table class= "appetizer" align=center>

    <thead align=center>
      <% course.dishes.each do |dish| %>
      <th><%= link_to "#{dish.name}", edit_dish_path(dish) %></th>
      <th>$<%= dish.price %></th>
        <td><%= dish.description %></td>
      </tr>

      <tr>
      <% end %>
      <%= link_to 'Add to ' + course.name, new_dish_path(course_id: course.id)%>
      <br>
    </thead>
  </table>
  <br>
<% end %>
<%= image_tag asset_url('escargot.jpg'), class: "snail" %>
<%= image_tag asset_url('lobsterbisque.jpg'), class: "seasnail" %>
<%= image_tag asset_url('creme.jpg'), class: "snailtrail" %>
